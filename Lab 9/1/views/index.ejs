<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lab 9 - 1</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
</head>
<body style="display: flex; flex-direction: column; align-items: center; margin-top: 50px;">
    <h1 style="color: #0086AB;">Users List</h1>
    <div style="border: 2px solid rgb(0, 134, 171); padding: 20px; width: 80%; border-radius: 20px; overflow: hidden; margin-bottom: 50px;">
        <table class="table table-condensed" style="border-collapse: collapse;">
            <thead>
                <tr style="font-weight: 600; text-align: center;">
                    <td>ID</td>
                    <td>First Name</td>
                    <td>Last Name</td>
                    <td>Username</td>
                    <td>Email</td>
                    <td>Actions</td>
                </tr>
            </thead>
            <tbody>
                <%users.forEach(user => { %>
                    <tr>
                        <td><%= user.id %></td>
                        <td><%= user.first_name %></td>
                        <td><%= user.last_name %></td>
                        <td><%= user.username %></td>
                        <td><%= user.email %></td>
                        <td><a href="/detail?id=<%= user.id %>" class="btn btn-info" role="button">Detail</a></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>